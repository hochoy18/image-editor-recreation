# Supabase Google 登录测试指南

## 🚀 启动测试

1. **启动开发服务器**：
```bash
npm run dev
```

2. **访问测试页面**：
打开浏览器访问：**http://localhost:3000/test-auth**

## 📋 测试步骤

### 步骤 1：测试 Google 登录

1. 在测试页面点击右上角的 **"Sign in with Google"** 按钮
2. 浏览器会跳转到 Google 授权页面
3. 选择你的 Google 账号并授权
4. 授权成功后会自动返回测试页面

**预期结果**：
- ✅ 页面刷新后显示 "已登录" 状态
- ✅ 显示用户邮箱
- ✅ 显示用户 ID
- ✅ 显示用户元数据（头像、姓名等）
- ✅ 显示 Token 验证成功信息
- ✅ 右上角显示用户头像下拉菜单

### 步骤 2：测试 Token 验证

查看测试页面中的 **"Token 验证状态"** 卡片：

**预期结果**：
- ✅ 显示 "Token 有效且已验证"
- ✅ 显示完整的 JWT Claims 信息
- ✅ 包含 `sub`（用户 ID）、`email`、`role` 等字段

### 步骤 3：测试用户菜单

1. 点击右上角的用户头像
2. 查看下拉菜单

**预期结果**：
- ✅ 显示用户邮箱和姓名
- ✅ 显示 "Sign out" 按钮

### 步骤 4：测试登出功能

1. 点击用户头像
2. 选择 **"Sign out"** 按钮

**预期结果**：
- ✅ 页面刷新后显示 "未登录" 状态
- ✅ 用户信息消失
- ✅ Token 验证失败或无 Token
- ✅ 右上角恢复显示 "Sign in with Google" 按钮

### 步骤 5：再次登录测试

重复步骤 1，验证：

**预期结果**：
- ✅ 可以重新登录
- ✅ 登录流程顺畅
- ✅ 用户信息正确显示

## 🔍 技术验证点

### 1. 服务器端验证
```typescript
// 在测试页面中，我们使用服务器端组件获取用户
const { data: { user } } = await supabase.auth.getUser()
```
- ✅ 验证服务器端能正确读取用户信息
- ✅ 验证 Cookie 正确传递

### 2. JWT Claims 验证
```typescript
const claims = await supabase.auth.getClaims()
```
- ✅ 验证 JWT 签名
- ✅ 确保安全的服务器端验证

### 3. 客户端状态管理
```typescript
// AuthState 组件使用客户端监听
supabase.auth.onAuthStateChange()
```
- ✅ 验证实时状态更新
- ✅ 验证 UI 自动响应登录状态变化

### 4. Token 刷新
- ✅ 通过 `proxy.ts` 自动刷新
- ✅ 用户无感知的 token 更新

## 🐛 常见问题排查

### 问题 1：登录后没有重定向
**解决方案**：
- 检查 `.env.local` 中的 `NEXT_PUBLIC_SITE_URL` 是否正确
- 确认 Supabase Dashboard 中配置的重定向 URL 包含 `http://localhost:3000/auth/callback`

### 问题 2：显示 "Token 验证失败"
**解决方案**：
- 检查 Google OAuth 配置是否正确
- 确认 Supabase 项目中 Google Provider 已启用
- 查看浏览器控制台的错误信息

### 问题 3：登录按钮不工作
**解决方案**：
- 打开浏览器开发者工具查看控制台错误
- 确认开发服务器正在运行
- 检查端口是否正确（默认 3000）

### 问题 4：登出后仍显示登录状态
**解决方案**：
- 清除浏览器 Cookies
- 刷新页面
- 检查 `/auth/logout` API 路由是否正常工作

## 📊 测试清单

使用此清单确保所有功能正常：

- [ ] Google 登录按钮可见并可点击
- [ ] 跳转到 Google 授权页面
- [ ] 授权成功后返回测试页面
- [ ] 显示用户邮箱
- [ ] 显示用户 ID
- [ ] 显示用户头像（如果有）
- [ ] Token 验证成功
- [ ] 用户菜单正常工作
- [ ] 登出功能正常
- [ ] 登出后返回未登录状态
- [ ] 可以重新登录
- [ ] 页面刷新后登录状态保持

## 🎯 成功标准

如果以上所有测试都通过，说明：

1. ✅ Supabase Google OAuth 配置正确
2. ✅ 服务器端认证工作正常
3. ✅ 客户端状态管理工作正常
4. ✅ Token 自动刷新机制正常
5. ✅ 登出功能正常
6. ✅ 安全的 JWT 验证正常

## 📝 下一步

测试通过后，你可以：

1. 在其他页面中使用 `<AuthState />` 组件
2. 使用服务器端代码保护需要登录的路由
3. 访问用户数据进行个性化功能开发
4. 集成到你的应用主流程中

---

**需要帮助？** 查看 `SUPABASE_AUTH_SETUP.md` 获取更多详细信息。
